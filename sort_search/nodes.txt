

排序总结：
    1.对于有序和无序列表，顺序搜索是O(n)
    2.在最坏的情况下，有序列表的二分查找是 O(log^n )
    3.哈希表可以提供恒定时间搜索
    4.冒泡排序，选择排序和插入排序是O(n^2)算法
    5.shell排序通过排序增量子列表来改进插入排序。它落在O(n)和O(n^2)之间
    6.归并排序是 O(nlog^n），但是合并过程需要额外的空间
    7.快速排序是O(nlog^n），但如果分割点不在列表中间附近，可能会降级到O(n^2)
      它不需要额外的空间




